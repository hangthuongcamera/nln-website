<div class="bg-white rounded-lg border border-gray-200 hover:shadow-xl transition-shadow duration-300 flex flex-col group relative overflow-hidden h-full">
    <% if (product.oldPrice && product.oldPrice > product.retailPrice) { %>
        <span class="absolute top-2 left-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded z-10">-<%= Math.round((1 - product.retailPrice/product.oldPrice)*100) %>%</span>
    <% } else if (product.flags && product.flags.isNewProduct) { %>
        <span class="absolute top-2 left-2 bg-green-500 text-white text-xs font-bold px-2 py-1 rounded z-10">MỚI</span>
    <% } %>
    <div class="absolute top-2 right-2 flex flex-col gap-2 z-10">
        <!-- Wishlist Button -->
        <button class="wishlist-btn bg-white/80 backdrop-blur-sm text-gray-600 hover:text-red-500 w-9 h-9 rounded-full shadow-md transition-colors flex items-center justify-center"
                data-product-id="<%= product._id %>"
                onclick="toggleWishlist(this, '<%= product._id %>')"
                title="Thêm vào yêu thích">
            <i class="fa-regular fa-heart"></i>
        </button>
        <!-- Compare Button -->
        <button class="compare-btn bg-white/80 backdrop-blur-sm text-gray-600 hover:text-primary w-9 h-9 rounded-full shadow-md transition-colors flex items-center justify-center"
                data-product-id="<%= product._id %>"
                onclick="toggleCompare(this, '<%= product._id %>')"
                title="So sánh sản phẩm">
            <i class="fa-regular fa-code-compare"></i>
        </button>
    </div>
    
    <a href="/product/<%= product.slug %>" class="block relative pt-[100%] overflow-hidden">
        <img src="<%= product.images && product.images.length > 0 ? product.images[0] : '/img/placeholder.png' %>" alt="<%= product.name %>" class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
    </a>
    
    <div class="p-3 md:p-4 flex flex-col flex-grow">
        <p class="text-xs text-gray-500 mb-1 font-mono">Mã: <%= product.sku || 'Đang cập nhật' %></p>
        <a href="/product/<%= product.slug %>" class="text-sm md:text-base font-semibold text-gray-800 hover:text-primary line-clamp-2 mb-2 h-10 md:h-12"><%= product.name %></a>
        <div class="mt-auto mb-3">
            <span class="text-lg font-bold text-red-600"><%= product.retailPrice ? product.retailPrice.toLocaleString('vi-VN') + 'đ' : 'Liên hệ' %></span>
            <% if (product.oldPrice && product.oldPrice > product.retailPrice) { %>
                <span class="text-xs text-gray-400 line-through block"><%= product.oldPrice.toLocaleString('vi-VN') %>đ</span>
            <% } %>
        </div>
        <div class="flex flex-col gap-2 mt-auto">
            <button type="button" 
                data-id="<%= product.slug %>" 
                data-name="<%= product.name ? product.name.replace(/"/g, '&quot;') : 'Sản phẩm' %>" 
                data-sku="<%= product.sku || 'Đang cập nhật' %>" 
                data-price="<%= product.retailPrice || 0 %>" 
                data-image="<%= product.images && product.images.length > 0 ? product.images[0] : '/img/placeholder.png' %>"
                onclick="addCurrentProductToCart(this)" 
                class="w-full bg-white border border-primary text-primary hover:bg-primary hover:text-white py-1.5 md:py-2 rounded text-sm font-medium transition-colors flex items-center justify-center">
                <i class="fa-solid fa-cart-plus mr-1.5"></i> Thêm vào giỏ
            </button>
        </div>
    </div>
</div>