        <!-- ==================== TOPBAR ==================== -->
        <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-4 sm:px-6 lg:px-8 z-10 shadow-sm w-full">
            <div class="flex items-center">
                <!-- Mobile Sidebar Toggle -->
                <button onclick="toggleSidebar()" class="text-gray-500 hover:text-primary focus:outline-none lg:hidden mr-4">
                    <i class="fa-solid fa-bars text-xl"></i>
                </button>
                <h1 class="text-lg font-bold text-gray-800 hidden sm:block"><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Dashboard Tổng Quan' %></h1>
            </div>
            
            <div class="flex items-center gap-4">
                <button class="text-gray-400 hover:text-primary transition-colors relative">
                    <i class="fa-regular fa-bell text-xl"></i>
                    <span class="absolute -top-1 -right-1 bg-red-500 text-white w-4 h-4 rounded-full text-[10px] flex items-center justify-center font-bold border border-white">3</span>
                </button>
                <div class="h-8 w-px bg-gray-200 mx-2"></div>
                <div class="flex items-center gap-3 cursor-pointer group">
                    <img src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?auto=format&fit=crop&q=80&w=100" alt="Avatar Admin" class="w-9 h-9 rounded-full object-cover border-2 border-gray-200 group-hover:border-primary transition-colors">
                    <div class="hidden md:block"><p class="text-sm font-bold text-gray-800 leading-tight">Admin System</p><p class="text-xs text-gray-500">Quản trị viên</p></div>
                    <!-- Nút Đăng xuất -->
                    <button onclick="logoutAdmin()" class="ml-2 text-gray-400 hover:text-red-500 transition px-2" title="Đăng xuất"><i class="fa-solid fa-arrow-right-from-bracket"></i></button>
                </div>
            </div>
        </header>